/**
 * @file YYC3-XY-脚本类-脚本使用及介绍文档.md
 * @description YYC³小语项成长守护体系脚本功能说明与使用指南
 * @author YYC³
 * @version 1.0.0
 * @created 2025-12-28
 * @updated 2025-12-28
 * @status published
 * @tags 脚本文档,使用指南,成长守护体系
 */

# YYC³小语项成长守护体系脚本使用及介绍文档

## 概述

YYC³小语项成长守护体系脚本集是一套基于Python开发的成长记录管理系统，旨在为0-22岁的青少年提供全方位的成长记录、分析和指导。该系统遵循「五高五标五化」核心理念，融合河洛文化元素，提供科学的成长阶段划分和个性化的成长建议。

本文档详细介绍了系统中的四个核心脚本及其使用方法。

---

## 脚本列表

### 1. my.py - 成长系统核心实现

#### 功能描述

`my.py`实现了`MuyuGrowthSystem`类，是整个成长守护体系的核心模块。该脚本负责生成完整的成长记录文件树结构，按照年龄阶段（0-21岁）自动创建目录和内容模板，并融入河洛文化元素。

#### 核心特性

- **年龄阶段管理**：覆盖0-21岁全成长周期，每个年龄段有独立的主题和配置
- **文化元素集成**：融合河洛文化、牡丹国色、言启智云等文化符号
- **多维度发展记录**：包括感知启蒙、学科启智、社交成长、健康护航、国学传承等维度
- **角色任务系统**：支持记录者、引导者、陪伴者、守护者、传承者、观察者等多元角色
- **年度成长志**：每个年龄段自动生成年度成长总结文档

#### 依赖项

```python
import os
from datetime import datetime
from typing import Dict, List, Any
```

#### 使用方法

**基本用法：**

```python
from my import MuyuGrowthSystem

# 创建成长系统实例
system = MuyuGrowthSystem(root_dir="沫语成长守护体系")

# 生成完整成长文件树
system.generate_growth_tree()

# 生成特定年龄的内容
system.generate_growth_tree_for_age(5)  # 生成5岁相关内容
```

**自定义配置：**

```python
# 自定义根目录
system = MuyuGrowthSystem(root_dir="自定义成长记录")

# 访问核心元素配置
core_elements = system.core_elements
print(core_elements["人物"])  # 输出：小龙女沫语（射手座·成长守护使）
print(core_elements["文化基底"])  # 输出：河洛文化·古都洛阳
```

**年龄阶段查询：**

```python
# 获取特定年龄的阶段名称
age_stage = system.core_elements["age_stages"][7]
print(age_stage)  # 输出：7岁_学科启途

# 获取特定年龄段的发展维度
dimensions = system.core_elements["development_dimensions"]["7-12岁"]
print(dimensions)  # 输出：['学科启智云', '兴趣深耕坊', '社交成长营', '健康护航队', '国学传承阁']
```

#### 生成的文件结构

```
沫语成长守护体系/
├── 核心信息.json
├── 0岁_启元初绽/
│   ├── 0岁_年度成长志.md
│   ├── 感知启蒙舱/
│   ├── 亲子共育录/
│   ├── 河洛自然志/
│   ├── 健康守护站/
│   └── 生日纪念册/
├── 1岁_萌智初醒/
├── ...
└── 21岁_毕业启程/
```

#### 核心方法说明

| 方法名 | 参数 | 返回值 | 说明 |
|--------|------|--------|------|
| `generate_growth_tree()` | 无 | None | 生成完整的成长文件树（0-21岁） |
| `generate_growth_tree_for_age(age)` | age: int | None | 生成特定年龄的文件树 |
| `_generate_0_3(age, path)` | age: int, path: str | None | 生成0-3岁阶段内容 |
| `_generate_4_6(age, path)` | age: int, path: str | None | 生成4-6岁阶段内容 |
| `_generate_7_12(age, path)` | age: int, path: str | None | 生成7-12岁阶段内容 |
| `_generate_teen(age, path)` | age: int, path: str | None | 生成13-18岁阶段内容 |
| `_generate_adult(age, path)` | age: int, path: str | None | 生成19-21岁阶段内容 |

#### 使用场景

1. **新建成长记录系统**：首次使用时，调用`generate_growth_tree()`创建完整目录结构
2. **补充特定年龄段**：当需要为特定年龄补充记录时，使用`generate_growth_tree_for_age(age)`
3. **文化教育集成**：通过访问`core_elements`获取文化符号和主题，用于教育场景
4. **个性化定制**：修改`core_elements`配置，实现个性化的成长记录体系

---

### 2. 沫语成长守护体系_统一成长记录系统.py - 系统基础设施

#### 功能描述

该脚本提供了成长记录系统的核心基础设施，包括日志记录、系统监控、数据持久化、性能监控等模块。采用单例模式确保系统组件的唯一性，支持数据加密、备份恢复等高级功能。

#### 核心特性

- **系统日志记录**：支持多级别日志（DEBUG/INFO/WARNING/ERROR），文件和控制台双输出
- **系统监控**：实时监控系统健康状态、资源使用情况、性能指标
- **数据持久化**：支持数据加密存储、版本控制、自动备份
- **性能监控**：通过装饰器实现函数执行时间监控和性能统计
- **错误处理**：统一的异常捕获和错误记录机制
- **线程安全**：使用锁机制确保多线程环境下的数据安全

#### 依赖项

```python
import os
import json
import logging
import threading
import base64
from datetime import datetime
from typing import Dict, List, Any, Optional, Callable
from functools import wraps
import time
```

#### 使用方法

**初始化系统日志：**

```python
from 沫语成长守护体系_统一成长记录系统 import SystemLogger

# 创建日志记录器（单例模式）
logger = SystemLogger(log_dir="logs")

# 记录不同级别的日志
logger.info("系统启动成功")
logger.debug("调试信息")
logger.warning("警告信息")
logger.error("错误信息", exception=Exception("测试异常"))
```

**系统监控使用：**

```python
from 沫语成长守护体系_统一成长记录系统 import SystemMonitor

# 创建系统监控器
monitor = SystemMonitor(logger)

# 获取系统健康状态
health_status = monitor.get_health_status()
print(health_status)
# 输出示例：
# {
#     "status": "healthy",
#     "timestamp": "2025-12-28T10:30:00",
#     "cpu_usage": 15.2,
#     "memory_usage": 45.8,
#     "disk_usage": 32.1
# }

# 获取性能指标
metrics = monitor.get_performance_metrics()
print(metrics)

# 添加自定义监控指标
monitor.add_custom_metric("user_count", 100)
monitor.add_custom_metric("active_sessions", 25)
```

**数据持久化使用：**

```python
from 沫语成长守护体系_统一成长记录系统 import DataPersistenceManager

# 创建数据持久化管理器
data_manager = DataPersistenceManager(logger, data_dir="data")

# 保存数据（普通模式）
data_manager.save_data("user_profile", {
    "name": "小龙女沫语",
    "age": 5,
    "stage": "5岁_语枢萌芽"
})

# 保存数据（加密模式）
data_manager.save_data("sensitive_info", {
    "id_card": "********",
    "phone": "********"
}, encrypt=True)

# 读取数据
profile = data_manager.load_data("user_profile")
print(profile)

# 创建备份
data_manager.create_backup()

# 恢复数据
data_manager.restore_backup("backup_20251228_103000.json")
```

**性能监控装饰器使用：**

```python
from 沫语成长守护体系_统一成长记录系统 import monitor_performance, log_errors

# 使用性能监控装饰器
@monitor_performance(logger)
def process_growth_data(data):
    # 处理成长数据的逻辑
    time.sleep(0.5)  # 模拟耗时操作
    return {"processed": True}

# 使用错误处理装饰器
@log_errors(logger)
def risky_operation():
    # 可能抛出异常的操作
    raise ValueError("测试错误")

# 调用函数
result = process_growth_data({"age": 5})
```

#### 核心类说明

| 类名 | 说明 | 主要方法 |
|------|------|----------|
| `SystemLogger` | 系统日志记录器（单例） | `info()`, `debug()`, `warning()`, `error()` |
| `SystemMonitor` | 系统监控器 | `get_health_status()`, `get_performance_metrics()`, `add_custom_metric()` |
| `DataPersistenceManager` | 数据持久化管理器 | `save_data()`, `load_data()`, `create_backup()`, `restore_backup()` |
| `CacheManager` | 缓存管理器 | `get()`, `set()`, `delete()`, `clear()` |

#### 使用场景

1. **系统初始化**：在应用启动时初始化日志和监控组件
2. **数据安全存储**：对敏感数据使用加密模式保存
3. **性能优化**：通过监控装饰器识别性能瓶颈
4. **系统健康检查**：定期检查系统状态，确保稳定运行
5. **数据备份恢复**：定期创建数据备份，防止数据丢失

---

### 3. 沫语成长守护体系_统一成长记录系统_整合版.py - 整合版本

#### 功能描述

该脚本是系统的整合版本，将多个功能模块集成到一个统一的框架中，提供更加完整和易用的成长记录系统。整合版本包含AI集成、版本控制、配置管理、缓存管理等高级功能。

#### 核心特性

- **统一配置管理**：使用数据类（dataclass）管理系统配置
- **AI集成管理**：提供AI辅助的成长数据分析和建议
- **版本控制系统**：支持数据版本管理和历史记录
- **智能缓存管理**：LRU缓存策略，提升系统性能
- **成长阶段枚举**：使用枚举类型定义成长阶段，提高代码可读性
- **多角色支持**：支持记录者、引导者、陪伴者、守护者、传承者、观察者等角色

#### 依赖项

```python
import os
import json
import logging
import threading
import hashlib
from datetime import datetime
from typing import Dict, List, Any, Optional
from dataclasses import dataclass, field
from enum import Enum
```

#### 使用方法

**初始化系统配置：**

```python
from 沫语成长守护体系_统一成长记录系统_整合版 import GrowthSystemConfig, GrowthStage

# 创建默认配置
config = GrowthSystemConfig()

# 自定义配置
config = GrowthSystemConfig(
    system_version="3.0.0",
    root_dir="沫语成长守护体系_v3"
)

# 访问配置信息
print(config.system_version)
print(config.core_elements["人物"])
```

**使用成长阶段枚举：**

```python
from 沫语成长守护体系_统一成长记录系统_整合版 import GrowthStage

# 获取特定年龄的成长阶段
stage = GrowthStage.get_stage_by_age(5)
print(stage)  # 输出：GrowthStage.AGE_5

# 获取阶段名称
stage_name = stage.get_stage_name()
print(stage_name)  # 输出：5岁_语枢萌芽

# 获取阶段主题
theme = stage.get_theme()
print(theme)  # 输出：语枢萌芽·兴趣探索期

# 遍历所有成长阶段
for age in range(0, 22):
    stage = GrowthStage.get_stage_by_age(age)
    print(f"{age}岁: {stage.get_stage_name()}")
```

**AI集成使用：**

```python
from 沫语成长守护体系_统一成长记录系统_整合版 import AIIntegrationManager

# 创建AI集成管理器
ai_manager = AIIntegrationManager(logger, cache)

# 分析成长数据
growth_data = {
    "age": 8,
    "height": 130,
    "weight": 28,
    "interests": ["阅读", "绘画", "音乐"],
    "academic_performance": {
        "math": 85,
        "chinese": 90,
        "english": 78
    }
}

analysis_result = ai_manager.analyze_growth_data(growth_data)
print(analysis_result)
# 输出示例：
# {
#     "analysis_timestamp": "2025-12-28T10:30:00",
#     "growth_trends": ["学科深耕期：关注学科基础建设"],
#     "recommendations": ["建议加强英语学习", "继续保持阅读习惯"],
#     "risk_alerts": ["注意平衡各科发展"]
# }
```

**版本控制使用：**

```python
from 沫语成长守护体系_统一成长记录系统_整合版 import VersionControlManager

# 创建版本控制管理器
vc_manager = VersionControlManager(logger, data_dir="data")

# 创建新版本
version_id = vc_manager.create_version("user_profile", {
    "name": "小龙女沫语",
    "age": 5
})
print(version_id)  # 输出：v1.0.0

# 获取版本历史
history = vc_manager.get_version_history("user_profile")
print(history)

# 恢复到指定版本
vc_manager.restore_version("user_profile", "v1.0.0")
```

**缓存管理使用：**

```python
from 沫语成长守护体系_统一成长记录系统_整合版 import CacheManager

# 创建缓存管理器
cache = CacheManager(max_size=100, ttl=3600)

# 设置缓存
cache.set("user_123", {"name": "沫语", "age": 5})

# 获取缓存
user_data = cache.get("user_123")
print(user_data)

# 删除缓存
cache.delete("user_123")

# 清空所有缓存
cache.clear()
```

#### 核心类说明

| 类名 | 说明 | 主要方法 |
|------|------|----------|
| `GrowthSystemConfig` | 系统配置数据类 | 配置属性访问 |
| `GrowthStage` | 成长阶段枚举 | `get_stage_by_age()`, `get_stage_name()`, `get_theme()` |
| `AIIntegrationManager` | AI集成管理器 | `analyze_growth_data()`, `_analyze_trends()`, `_generate_recommendations()` |
| `VersionControlManager` | 版本控制管理器 | `create_version()`, `get_version_history()`, `restore_version()` |
| `CacheManager` | 缓存管理器 | `get()`, `set()`, `delete()`, `clear()` |

#### 使用场景

1. **系统集成**：使用整合版本快速搭建完整的成长记录系统
2. **AI辅助分析**：利用AI功能分析成长数据，获取个性化建议
3. **数据版本管理**：对重要数据进行版本控制，支持历史回溯
4. **性能优化**：使用缓存管理器提升系统响应速度
5. **配置管理**：通过配置类统一管理系统参数

---

### 4. 沫语成长树创建.py - 文件夹结构生成

#### 功能描述

该脚本专注于创建成长记录的文件夹结构和模板文件，按照不同成长阶段（胎儿期、新生儿期、婴儿期、幼儿期、学龄前、小学、中学、大学）生成对应的目录结构和记录模板。

#### 核心特性

- **阶段化文件夹结构**：按照8个成长阶段创建独立的文件夹
- **模板文件生成**：为每个阶段生成专用的记录模板
- **阶段寄语**：为每个阶段提供文化寄语和成长指导
- **灵活的创建逻辑**：支持增量创建，避免覆盖已有文件
- **文化元素融入**：在寄语中融入河洛文化和成长理念

#### 依赖项

```python
import os
from datetime import datetime
```

#### 使用方法

**创建完整文件夹结构：**

```python
from 沫语成长树创建 import create_folder_structure

# 创建完整的成长文件夹结构
main_path = "沫语成长树"
create_folder_structure(main_path)

# 生成的结构：
# 沫语成长树/
# ├── 胎儿期/
# │   ├── 产检记录/
# │   ├── 孕期B超/
# │   ├── 胎教内容/
# │   ├── 孕妇日记/
# │   └── 阶段寄语.txt
# ├── 新生儿期/
# ├── 婴儿期/
# ├── 幼儿期/
# ├── 学龄前/
# ├── 小学/
# ├── 中学/
# └── 大学/
```

**创建特定阶段的模板：**

```python
from 沫语成长树创建 import create_templates

# 为婴儿期创建模板
create_templates("沫语成长树/婴儿期", "婴儿期")

# 为小学阶段创建模板
create_templates("沫语成长树/小学", "小学")

# 为大学阶段创建模板
create_templates("沫语成长树/大学", "大学")
```

**自定义阶段寄语：**

```python
from 沫语成长树创建 import generate_phase_message

# 生成阶段寄语
message = generate_phase_message("婴儿期")
print(message)

# 输出示例：
# 🌸 婴儿期寄语
# 
# 亲爱的宝贝，这是你探索世界的起点。
# 每一次翻身、每一个笑容，都是成长的奇迹。
# 让我们用爱记录下这些珍贵的瞬间。
```

#### 文件夹结构说明

| 阶段 | 子文件夹 | 说明 |
|------|----------|------|
| 胎儿期 | 产检记录、孕期B超、胎教内容、孕妇日记 | 记录孕期健康和胎教活动 |
| 新生儿期 | 护理记录、健康档案、成长照片、脐带护理 | 记录新生儿护理和健康 |
| 婴儿期 | 喂养记录、疫苗接种、发育指标、早教活动 | 记录婴儿喂养和早期教育 |
| 幼儿期 | 语言发展、认知能力、社交活动、兴趣爱好 | 记录幼儿能力发展 |
| 学龄前 | 学前准备、安全教育、习惯养成、亲子游戏 | 记录学前准备和习惯培养 |
| 小学 | 学习记录、课外活动、成绩档案、教师评语 | 记录小学学习和活动 |
| 中学 | 学科笔记、考试成绩、社会实践、升学规划 | 记录中学学习和规划 |
| 大学 | 专业学习、实习经历、职业规划、毕业准备 | 记录大学学习和职业准备 |

#### 模板文件说明

**婴儿期模板示例：**

```text
首次事件记录模板.txt
----------------------
日期：
事件：（如第一次翻身/第一次笑出声）
场景：
表现：（表情/动作/声音）
家长感受：
```

**小学阶段模板示例：**

```text
周小结模板.txt
----------------------
本周最开心的事：
遇到的小困难：
学会的新东西：
想对爸妈说的话：
```

#### 使用场景

1. **新建成长记录系统**：首次使用时，调用`create_folder_structure()`创建完整目录
2. **补充特定阶段**：当需要为特定阶段补充记录时，使用`create_templates()`
3. **个性化定制**：修改`folder_structure`字典，自定义文件夹结构
4. **文化教育**：通过阶段寄语融入文化元素，进行文化教育

---

## 脚本关系与集成

### 脚本依赖关系图

```
沫语成长守护体系_统一成长记录系统_整合版.py (整合版本)
    ├── 沫语成长守护体系_统一成长记录系统.py (基础设施)
    │   ├── SystemLogger (日志记录)
    │   ├── SystemMonitor (系统监控)
    │   └── DataPersistenceManager (数据持久化)
    ├── my.py (核心实现)
    │   └── MuyuGrowthSystem (成长系统)
    └── 沫语成长树创建.py (文件夹创建)
        └── create_folder_structure() (文件夹生成)
```

### 推荐使用顺序

1. **第一步**：使用`沫语成长守护体系_统一成长记录系统.py`初始化系统基础设施
2. **第二步**：使用`my.py`生成核心成长记录文件树
3. **第三步**：使用`沫语成长树创建.py`补充特定阶段的模板文件
4. **第四步**：使用`沫语成长守护体系_统一成长记录系统_整合版.py`进行高级功能操作

### 集成使用示例

```python
# 1. 初始化系统基础设施
from 沫语成长守护体系_统一成长记录系统 import SystemLogger, SystemMonitor

logger = SystemLogger(log_dir="logs")
monitor = SystemMonitor(logger)

# 2. 生成核心成长记录文件树
from my import MuyuGrowthSystem

system = MuyuGrowthSystem(root_dir="沫语成长守护体系")
system.generate_growth_tree()

# 3. 补充特定阶段的模板
from 沫语成长树创建 import create_templates

create_templates("沫语成长守护体系/5岁_语枢萌芽", "幼儿期")

# 4. 使用整合版本进行高级操作
from 沫语成长守护体系_统一成长记录系统_整合版 import (
    AIIntegrationManager,
    VersionControlManager,
    CacheManager
)

cache = CacheManager()
ai_manager = AIIntegrationManager(logger, cache)

# 分析成长数据
growth_data = {"age": 5, "height": 110, "weight": 18}
analysis = ai_manager.analyze_growth_data(growth_data)
print(analysis)
```

---

## 系统配置与环境要求

### Python版本要求

- **最低版本**：Python 3.7+
- **推荐版本**：Python 3.9+

### 依赖库

所有脚本均使用Python标准库，无需额外安装第三方依赖。

### 目录结构要求

```
项目根目录/
├── docs/
│   └── YYC3-XY-脚本类/
│       ├── my.py
│       ├── 沫语成长守护体系_统一成长记录系统.py
│       ├── 沫语成长守护体系_统一成长记录系统_整合版.py
│       └── 沫语成长树创建.py
├── logs/                    # 日志目录（自动创建）
├── data/                    # 数据目录（自动创建）
│   └── backups/            # 备份目录（自动创建）
└── 沫语成长守护体系/        # 成长记录目录（自动创建）
```

### 环境变量（可选）

```bash
# 日志目录
export YYC3_LOG_DIR="logs"

# 数据目录
export YYC3_DATA_DIR="data"

# 成长记录根目录
export YYC3_ROOT_DIR="沫语成长守护体系"
```

---

## 最佳实践

### 1. 日志管理

```python
# 建议在应用启动时初始化日志
logger = SystemLogger(log_dir="logs")

# 使用不同级别的日志记录
logger.info("系统启动")
logger.debug("调试信息")
logger.warning("警告信息")
logger.error("错误信息", exception=e)
```

### 2. 数据安全

```python
# 对敏感数据使用加密模式
data_manager.save_data("sensitive_info", data, encrypt=True)

# 定期创建备份
data_manager.create_backup()

# 定期清理过期备份
data_manager.cleanup_old_backups(days=30)
```

### 3. 性能优化

```python
# 使用缓存管理器提升性能
cache = CacheManager(max_size=100, ttl=3600)

# 对频繁访问的数据使用缓存
result = cache.get(cache_key)
if result is None:
    result = expensive_operation()
    cache.set(cache_key, result)
```

### 4. 错误处理

```python
# 使用装饰器统一处理错误
@log_errors(logger)
def risky_function():
    # 可能抛出异常的操作
    pass
```

### 5. 版本控制

```python
# 对重要数据进行版本控制
version_id = vc_manager.create_version("important_data", data)

# 定期检查版本历史
history = vc_manager.get_version_history("important_data")
```

---

## 常见问题

### Q1: 如何自定义成长阶段？

**A:** 修改`my.py`中的`core_elements["age_stages"]`字典，添加或修改年龄阶段配置。

### Q2: 如何添加新的文化元素？

**A:** 在`core_elements["文化符号"]`列表中添加新的文化符号，或在相关模板中引用。

### Q3: 如何备份数据？

**A:** 使用`DataPersistenceManager.create_backup()`方法创建备份，或手动复制`data/`目录。

### Q4: 如何恢复数据？

**A:** 使用`DataPersistenceManager.restore_backup(backup_file)`方法恢复指定备份。

### Q5: 如何查看系统日志？

**A:** 日志文件位于`logs/`目录，文件名格式为`system_YYYYMMDD.log`。

### Q6: 如何监控系统性能？

**A:** 使用`SystemMonitor.get_performance_metrics()`方法获取性能指标。

### Q7: 如何使用AI分析功能？

**A:** 使用`AIIntegrationManager.analyze_growth_data(data)`方法分析成长数据。

### Q8: 如何清理缓存？

**A:** 使用`CacheManager.clear()`方法清空所有缓存。

---

## 技术支持

如有问题或建议，请联系：

- **邮箱**：admin@0379.email
- **项目**：YYC³小语项成长守护体系

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0.0 | 2025-12-28 | 初始版本，包含四个核心脚本 |

---

<div align="center">

> 「***YanYuCloudCube***」
> 「***<admin@0379.email>***」
> 「***Words Initiate Quadrants, Language Serves as Core for the Future***」
> 「***All things converge in the cloud pivot; Deep stacks ignite a new era of intelligence***」

</div>
